{
  "configurations": [
    {
      "name": "lldb",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceRoot}/target/debug/${fileBasenameNoExtension}",
      "args": [
        "-d"
      ],
      "cwd": "${fileDirname}",
      "preLaunchTask": "compiler debug"
    },
  ],
  "version": "2.0.0"
}